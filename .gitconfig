# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Angelo Ashmore
	email = angeloashmore@users.noreply.github.com
[push]
	default = simple
[credential]
	helper = osxkeychain
[core]
	excludesfile = /Users/angeloashmore/.gitignore_global
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[pull]
	rebase = false
